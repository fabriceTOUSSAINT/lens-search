import mongoose from 'mongoose';


const Schema = mongoose.Schema({
  lens: String,
  manufacturer: String,
  year: Number,
  lens_mount: String
});

schema.statics.getLens = function(lensName){
  const currLens = '';
  Lens.find({ $text: {$search: ${lensName}} )
}


module.exports = Lens = mongoose.model('Lens', Schema);

/*
const
const <%= name %>Schema = new Schema({

});

export default mongoose.model('<%= helpers.capitalize(name) %>', <%= name %>Schema);
*/
